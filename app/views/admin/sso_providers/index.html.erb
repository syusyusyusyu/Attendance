<div class="max-w-6xl mx-auto space-y-6">
  <div class="card p-6">
    <h1 class="text-2xl font-semibold text-gray-900">SSO設定</h1>
    <p class="text-sm text-gray-500 mt-1">学校アカウント連携の設定を管理します。</p>
  </div>

  <div class="card p-6">
    <h2 class="text-sm font-medium text-gray-700 mb-3">新規追加</h2>
    <%= render "form", provider: @provider %>
  </div>

  <div class="card p-6">
    <h2 class="text-sm font-medium text-gray-700 mb-3">登録済み</h2>
    <div class="table-container">
      <table class="min-w-full divide-y divide-gray-200 text-sm">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">名称</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">方式</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">状態</th>
            <th class="px-4 py-2 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @providers.each do |provider| %>
            <tr>
              <td class="px-4 py-2 text-sm text-gray-700"><%= provider.name %></td>
              <td class="px-4 py-2 text-sm text-gray-500"><%= provider.strategy %></td>
              <td class="px-4 py-2 text-sm text-gray-500"><%= provider.enabled? ? "有効" : "無効" %></td>
              <td class="px-4 py-2 text-right">
                <%= link_to "編集", edit_admin_sso_provider_path(provider), class: "text-google-green text-sm" %>
              </td>
            </tr>
          <% end %>
          <% if @providers.empty? %>
            <tr>
              <td colspan="4" class="px-4 py-6 text-center text-sm text-gray-500">SSO設定がありません。</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
