<div class="max-w-6xl mx-auto space-y-6">
  <div class="card p-6">
    <h1 class="text-2xl font-semibold text-gray-900">端末管理</h1>
    <p class="text-sm text-gray-500 mt-1">公認端末の承認と利用状況を確認します。</p>
  </div>

  <div class="card p-6">
    <%= form_with url: admin_devices_path, method: :get, class: "flex flex-col sm:flex-row gap-3 items-end" do |f| %>
      <div>
        <%= f.label :status, "状態", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.select :status,
          options_for_select([["すべて", ""], ["承認済み", "approved"], ["未承認", "pending"]], @status),
          {},
          class: "input" %>
      </div>
      <div>
        <%= f.submit "絞り込む", class: "btn btn-secondary w-full" %>
      </div>
    <% end %>
  </div>

  <div class="card p-6">
    <div class="table-container">
      <table class="min-w-full divide-y divide-gray-200 text-sm">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ユーザー</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">端末名</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">状態</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">最終利用</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">IP</th>
            <th class="px-4 py-2 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @devices.each do |device| %>
            <tr>
              <td class="px-4 py-2 text-sm text-gray-700"><%= device.user&.name || "-" %></td>
              <td class="px-4 py-2 text-sm text-gray-500"><%= device.name || "未登録端末" %></td>
              <td class="px-4 py-2 text-sm text-gray-700"><%= device.approved? ? "承認済み" : "未承認" %></td>
              <td class="px-4 py-2 text-sm text-gray-500"><%= device.last_seen_at&.strftime("%Y-%m-%d %H:%M") || "-" %></td>
              <td class="px-4 py-2 text-sm text-gray-500"><%= device.ip || "-" %></td>
              <td class="px-4 py-2 text-right">
                <%= form_with url: admin_device_path(device), method: :patch do %>
                  <input type="hidden" name="approved" value="<%= device.approved? ? 0 : 1 %>" />
                  <button type="submit" class="btn btn-secondary text-sm">
                    <%= device.approved? ? "承認解除" : "承認" %>
                  </button>
                <% end %>
              </td>
            </tr>
          <% end %>
          <% if @devices.empty? %>
            <tr>
              <td colspan="6" class="px-4 py-6 text-center text-sm text-gray-500">端末がありません。</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
