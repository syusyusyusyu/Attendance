<% if turbo_frame_request? %>
  <%= render "form_drawer", title: "ユーザー作成", subtitle: "新規ユーザーの基本情報を入力します。", user: @user %>
<% else %>
  <div class="max-w-xl mx-auto space-y-6">
    <div class="card p-6">
      <h1 class="text-2xl font-semibold text-body">ユーザー作成</h1>
      <p class="text-sm text-body-secondary mt-1">新規ユーザーの基本情報を入力します。</p>
    </div>

    <div class="card p-6">
      <%= render "form", user: @user %>
    </div>
  </div>
<% end %>
