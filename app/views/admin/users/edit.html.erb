<% if turbo_frame_request? %>
  <%= render "form_drawer", title: "ユーザー編集", subtitle: nil, user: @user %>
<% else %>
  <div class="max-w-xl mx-auto space-y-6">
    <div class="card p-6">
      <h1 class="text-2xl font-semibold text-body">ユーザー編集</h1>
    </div>

    <div class="card p-6">
      <%= render "form", user: @user %>
    </div>
  </div>
<% end %>
