<%= turbo_frame_tag "school_class_form",
  class: "drawer-panel",
  data: { controller: "drawer", drawer_open_value: true } do %>
  <div class="card">
    <div class="p-4 bg-google-grey border-b border-gray-200">
      <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2">
        <div>
          <h2 class="text-xl font-medium text-gray-800"><%= title %></h2>
          <% if subtitle.present? %>
            <p class="text-sm text-gray-500 mt-1"><%= subtitle %></p>
          <% end %>
        </div>
        <button type="button" class="text-sm text-gray-500 hover:text-gray-700" data-action="drawer#close">
          閉じる
        </button>
      </div>
    </div>
    <div class="p-6">
      <%= form_with model: school_class, class: "space-y-6" do |f| %>
        <%= render "form", f: f %>
        <div class="flex justify-end">
          <%= f.submit submit_label, class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
