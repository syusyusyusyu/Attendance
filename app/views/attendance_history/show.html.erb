<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-semibold text-slate-900">出席履歴</h1>
    <p class="text-sm text-slate-500">日付を選択して出席状況を確認します。</p>
  </div>

  <div class="bg-white border border-slate-200 rounded-2xl p-6">
    <%= form_with url: history_path, method: :get, class: "flex flex-col md:flex-row md:items-end gap-4" do |f| %>
      <div>
        <%= f.label :date, "日付", class: "block text-sm font-medium text-slate-700" %>
        <%= f.date_field :date, value: @date, class: "mt-1 rounded-md border-slate-300 focus:border-emerald-500 focus:ring-emerald-500" %>
      </div>
      <%= f.submit "表示", class: "rounded-md bg-emerald-600 text-white px-6 py-2 hover:bg-emerald-500" %>
    <% end %>
  </div>

  <% if @records.any? %>
    <div class="bg-white border border-slate-200 rounded-2xl divide-y divide-slate-100">
      <% @records.each do |record| %>
        <div class="p-4 flex flex-col md:flex-row md:items-center md:justify-between">
          <div>
            <div class="font-semibold text-slate-900"><%= record.school_class.name %></div>
            <div class="text-sm text-slate-500"><%= record.school_class.room %></div>
          </div>
          <div class="mt-2 md:mt-0 text-sm">
            <span class="inline-flex items-center rounded-full px-3 py-1 <%= record.status == 'present' ? 'bg-emerald-100 text-emerald-800' : record.status == 'late' ? 'bg-amber-100 text-amber-800' : 'bg-rose-100 text-rose-800' %>">
              <%= record.status_label || record.status %>
            </span>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="bg-white border border-slate-200 rounded-2xl p-6 text-slate-500">
      この日の出席記録はありません。
    </div>
  <% end %>
</div>
