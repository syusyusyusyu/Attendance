<div class="max-w-2xl mx-auto">
  <div class="card">
    <div class="p-4 bg-google-grey border-b border-gray-200">
      <h2 class="text-xl font-medium text-gray-800">出席用QRコード生成</h2>
      <p class="text-sm text-gray-500 mt-1">授業を選択してQRコードを生成します。</p>
    </div>

    <div
      class="p-6"
      data-controller="qr"
      <% if @selected_class %>
        data-qr-expires-at-value="<%= @expires_at.to_i %>"
        data-qr-filename-value="qrcode-<%= @selected_class.id %>.svg"
        data-qr-refresh-url-value="<%= generate_qr_path(class_id: @selected_class.id, format: :json) %>"
        data-qr-refresh-interval-value="60"
      <% end %>
    >
      <div class="mb-6">
        <%= form_with url: generate_qr_path, method: :get, class: "space-y-4" do |f| %>
          <div>
            <%= f.label :class_id, "クラス", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= f.select :class_id,
              options_for_select(@classes.map { |klass| ["#{klass.name} (#{klass.room})", klass.id] }, @selected_class&.id),
              { include_blank: "クラスを選択" },
              class: "input" %>
          </div>
          <%= f.submit "生成", class: "btn btn-primary" %>
        <% end %>
      </div>

      <% if @selected_class %>
        <div class="flex flex-col items-center">
          <div class="bg-white p-4 rounded-lg border border-gray-300 mb-4 w-full max-w-xs qr-code" data-qr-target="code">
            <% qr = RQRCode::QRCode.new(@token) %>
            <%= qr.as_svg(module_size: 6, fill: "ffffff", color: "0f172a", shape_rendering: "crispEdges").html_safe %>
          </div>

          <div class="mb-4 text-center">
            <div class="text-sm font-medium text-gray-500 mb-1">クラス情報:</div>
            <div class="text-lg font-semibold"><%= @selected_class.name %></div>
            <div class="text-sm text-gray-600"><%= @selected_class.room %></div>
          </div>

          <div class="flex items-center mb-2">
            <svg class="h-5 w-5 text-google-red mr-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="9" />
              <path d="M12 7v5l3 3" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            <span class="text-sm text-gray-700">
              有効期限: <%= @expires_at.strftime("%H:%M") %> (残り <span data-qr-target="timeLeft"><%= ((@expires_at - Time.current).to_i.clamp(0, 300) / 60).to_s %>:<%= ((@expires_at - Time.current).to_i.clamp(0, 300) % 60).to_s.rjust(2, "0") %></span>)
            </span>
          </div>
          <p class="text-xs text-gray-500 mb-6">QRコードは定期的に自動更新されます。</p>

          <div class="flex space-x-3">
            <%= link_to generate_qr_path(class_id: @selected_class.id), class: "btn btn-secondary" do %>
              <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 12a9 9 0 1 1-3-6.7" stroke-linecap="round" stroke-linejoin="round" />
                <path d="M21 3v6h-6" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
              再生成
            <% end %>
            <button type="button" class="btn btn-blue" data-action="qr#download">
              <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" stroke-linecap="round" stroke-linejoin="round" />
                <path d="M7 10l5 5 5-5" stroke-linecap="round" stroke-linejoin="round" />
                <path d="M12 15V3" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
              ダウンロード
            </button>
          </div>
        </div>
      <% else %>
        <div class="text-center py-4 text-gray-500">
          クラスを選択するとQRコードが生成されます。
        </div>
      <% end %>
    </div>
  </div>
</div>
