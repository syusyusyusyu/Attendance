<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "RoR出席管理システム" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <meta name="theme-color" content="#5B5BD6">

    <link rel="icon" href="<%= asset_path('favicon.svg') %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Noto+Sans+JP:wght@400;500;700&display=swap"
      rel="stylesheet"
    >

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <a href="#main-content" class="skip-link">メインコンテンツへスキップ</a>
    <div class="flex flex-col min-h-screen" data-controller="sidebar">
      <% if current_user %>
        <%= render "shared/nav" %>
        <div class="flex flex-1 pt-[var(--app-header-height)]">
          <div
            data-sidebar-target="overlay"
            data-action="click->sidebar#close"
            class="fixed inset-0 z-20 hidden lg:hidden overlay-backdrop"
          ></div>
          <%= render "shared/sidebar" %>
          <main id="main-content" class="flex-1 w-full lg:ml-[var(--sidebar-width)] min-w-0 pb-20 sm:pb-0">
            <div class="max-w-[1600px] mx-auto p-6 md:p-8 lg:p-10 w-full">
              <div id="flash"><%= render "shared/flash" %></div>
              <%= yield %>
            </div>
          </main>
          <%# Bottom Navigation (SP専用: 全ロール対応) %>
          <%= render "shared/bottom_nav" %>
        </div>
      <% else %>
        <main id="main-content" class="flex-1">
          <%= yield %>
        </main>
      <% end %>
    </div>
    <script>
      if ("serviceWorker" in navigator) {
        navigator.serviceWorker.register("/service-worker.js")
      }
    </script>
  </body>
</html>
