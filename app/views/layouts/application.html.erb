<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "RoR出席システム" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="<%= asset_path('favicon.svg') %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.svg') %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&family=Noto+Sans+JP:wght@400;500;700&display=swap"
      rel="stylesheet"
    >

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-900">
    <div class="flex flex-col min-h-screen" data-controller="sidebar">
      <% if current_user %>
        <%= render "shared/nav" %>
        <div class="flex flex-1 pt-[var(--app-header-height)]">
          <div
            data-sidebar-target="overlay"
            data-action="click->sidebar#close"
            class="fixed inset-0 bg-gray-600 bg-opacity-50 z-20 hidden lg:hidden"
          ></div>
          <%= render "shared/sidebar" %>
          <main class="flex-1 w-full lg:ml-64 min-w-0">
            <div class="max-w-7xl mx-auto p-4 sm:px-6 lg:px-8">
              <%= render "shared/flash" %>
              <%= yield %>
            </div>
          </main>
        </div>
      <% else %>
        <main class="flex-1">
          <%= yield %>
        </main>
      <% end %>
    </div>
  </body>
</html>
